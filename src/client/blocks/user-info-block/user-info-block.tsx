import React, { ReactNode } from 'react';

export class UserInfoBlock extends React.PureComponent {
    render(): ReactNode {
        return (
            <div>

            </div>
        );
    }
}
