# https://www.dmosk.ru/miniinstruktions.php?mini=nginx-redirects

server {
    listen 80;
    server_name www.alfa-code.ru alfa-code.ru;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name www.alfa-code.ru alfa-code.ru;
    # server_name www.alfa-code.com alfa-code.com www.alfa-code.ru alfa-code.ru;

    ssl_certificate     /app/.build/tls/certificate.crt;
    ssl_certificate_key /app/.build/tls/private.key;
    ssl_protocols       TLSv1 TLSv1.1 TLSv1.2;
    ssl_ciphers         HIGH:!aNULL:!MD5;
    location / {
        proxy_pass http://localhost:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
