import React, { ReactNode } from 'react';

export class RecentCoursesBlock extends React.PureComponent {
    render(): ReactNode {
        return (
            <div>

            </div>
        );
    }
}
