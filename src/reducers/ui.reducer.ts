const initialState: any = {
    htmlTags: {}
}

export function uiReducer(state = initialState, action) {
    switch (action.type) {
      default:
        return {
            ...state
        }
    }
  }
