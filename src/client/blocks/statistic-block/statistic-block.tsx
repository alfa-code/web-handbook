import React, { ReactNode } from 'react';

export class StatisticBlock extends React.PureComponent {
    render(): ReactNode {
        return (
            <div>

            </div>
        );
    }
}
